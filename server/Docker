# Використовуйте офіційний образ Node.js як базовий образ
FROM node:14

# Встановіть робочу директорію в контейнері
WORKDIR /app

# Скопіюйте package.json та package-lock.json у робочу директорію
COPY package*.json ./

# Встановіть залежності
RUN npm install

# Скопіюйте всі файли сервера API в робочу директорію
COPY . .

# Виконайте команду для запуску сервера API
CMD ["node", "app.js"]